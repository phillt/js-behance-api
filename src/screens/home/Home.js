import React from "react";
import SearchContainer from "../../components/searchContainer/SearchContainer";

const Home = ({}) => {
    return (
        <div>
            <header>
                <SearchContainer />
            </header>

        </div>
    )
};

export default Home;